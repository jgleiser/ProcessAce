# Server Configuration
PORT="3000"
NODE_ENV="development"
LOG_LEVEL="info"

# LLM Configuration
# Configure keys for the providers you intend to use.
OPENAI_API_KEY="sk-..."
GOOGLE_API_KEY="AIza..."
ANTHROPIC_API_KEY="sk-ant-..."

# Optional: Override URL for local LLMs (e.g., Ollama, LocalAI) - used when provider is 'openai'
# LLM_PROVIDER_URL=http://localhost:11434/v1

# Dev/Testing
# Set to 'true' to use the internal mock provider (generates dummy BPMN without calling an API)
MOCK_LLM="false"
